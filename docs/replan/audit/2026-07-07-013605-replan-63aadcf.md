---
skill: replan
version: 1.3.1
timestamp: 2026-07-07T01:36:05+02:00
model: opus
plan_hash: 63aadcf
plan_ref:
  kind: path
  value: PLAN_2026-07-07.md (repo parent dir)
plan_lineage: [628e70b]
security_guidance:
  detected: false
  reason: none
autonomous_mode: false
lessons_injected_bytes: 750
lessons_truncated: false
---

## Plan summary (first 200 chars)
Plán oprav coworkings.cz: P0 RLS na 23 tabulkách + merge auth-emails, P1 COW.OS Stripe monetizace + e-mail triggery, P2 DELETE bug + UI díry, P3 landing sliby + úklid branchí.

## Agents dispatched
| Agent | Verdict | Critical | Important | Minor |
|---|---|---|---|---|
| Codebase alignment | ISSUES FOUND | 2 | 5 | 3 |
| Feasibility & risks | ISSUES FOUND | 4 | 4 | 4 |
| Security | ISSUES FOUND | 3 | 3 | 3 |
| Fresh perspective | CONCERNS | 3 | 4 | 4 |

## Per-agent findings (kritické)

### Codebase alignment
- Bod 3 chybný předpoklad: webhook nezná CowOsSubscription vůbec; checkout neposílá metadata.coworkingSlug — staví se nová větev, ne úprava.
- Bod 5: DELETE je soft-delete, route je korektní; příčina bude environmentální — nejdřív frontend fix, pak diagnóza.

### Feasibility & risks
- ensure-tables.ts + migrate.mjs tvoří tabulky za běhu bez RLS → RLS musí do runtime DDL, ne jen jednorázový ALTER.
- Lokální main 2 commity za origin/main → fetch+pull před merge.
- auth-emails veze i ne-auth změny (globals.css, paper-diary, marketplace, ownership fix).
- Chybí DB snapshot před RLS změnami.

### Security
- accept-transfer bez $transaction → replay token; console.log plného accept URL s tokenem.
- Anon klíč je NEXT_PUBLIC → doplnit REVOKE anon/authenticated nebo vypnout Data API (řeší i budoucí tabulky).
- metadata.userEmail/coworkingSlug bez ověření customer↔user vazby — ownership slugu ověřit při tvorbě Checkout session.

### Fresh perspective
- EMAIL_PROVIDER default=console — po merge maily nepoletí bez ENV na Vercelu.
- DB: 7 users, 1 free subscription, 0 members → P1 monetizace předčasná, doporučeno odložit za veřejné spuštění.
- Invite gate blokuje katalog+SEO, nic nechrání → strategické rozhodnutí nadřazené bugům.
- Chybí error tracking (Sentry), ověření záloh, žádné testy.

## Changes applied to plan
- P0 rozšířeno: DB snapshot, REVOKE anon/authenticated, RLS do runtime DDL (ensure-tables.ts, migrate.mjs).
- Nový P0 bod: fix accept-transfer (transakce + idempotence + nelogovat token).
- P0 merge: fetch/pull main, review celého diffu, ENV Resend + console-first deploy strategie; odstraněno neplatné riziko migrace (VerificationToken se reusuje).
- P1 přestavěno: nadřazeno rozhodnutí gate/launch + katalog; Stripe monetizace doporučena odložit, rozsah upřesněn (nová webhook větev, claim ownership check, price IDs, náhrada Unsafe SQL).
- P2: DELETE bug přeformulován (frontend-first); e-mail triggery upřesněny (šablony existují, props nesedí; transfer šablona chybí).
- Nový P3 bod: Sentry, zálohy, Stripe apiVersion, migrate.mjs silent-fail.
